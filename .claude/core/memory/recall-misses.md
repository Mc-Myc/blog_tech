# Recall misses — store

> Chaque fois qu'une recherche (grep INDEX) ne trouve PAS une info qui existait :
> une entrée ici. À 3 entrées : rouvrir ADR-002 (mémoire vectorielle différée).

## INDEX

(vide — aucun miss enregistré)
